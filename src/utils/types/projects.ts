export type Projects = {
    createdAt: Date,
    deployUrl: string,
    description: string,
    githubUrl: string,
    id: string;
    photo: {
        url: string,
    },
    tagsTechnologies: string,
    title: string,
    updatedAt: Date,
}